Etape 1: 
Installer "Docker Desktop"

Etape 2:
Ouvrir le terminal wsl et run la commande :
docker compose up -d --build => si c'est la première fois
docker compose up -d => si ce n'est pas la première fois

Pour afficher l'index.html:
click sur -> http://localhost:3000

Pour afficher la base de donnée:
click sur -> http://localhost:5050

\* Pour la première connexion */
dans le terminal : "docker compose down -v" (si jamais la version du docker est antérieur au maj sur la db)
puis : "docker compose up -d --build"
nom utilisateur : augustin.viemont@epitech.eu
mdp : viemont
click droit sur servers
click droit sur register
Server ...
name = postgres-db
click sur connection
Host name/adress = db
Port = 5432
Username = devuser
MDP = devpass